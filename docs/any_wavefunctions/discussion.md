# Discussion

At least one constraint was missed:

- the virtual pair is almost surely not a ground state.

Actually, this approach is nothing new. It is [Levy/Levy-Lieb(?)](https://physics.stackexchange.com/questions/69618/constrained-search-formulation-of-dft-by-levy-or-by-levy-lieb) constrained search.

By scanning through all possible virtual pairs one actually obtains a spectrum that contains the genuine kinetic energy $T$. It even should be the minimum?

!!! question "Measure"

    Does the space of phases $\alpha$ have a natural measure?
    
    If so, does the virtual kinetic energy spectrum (should be something like a probability density distribution) contain any interesting information?
    
    May there even be a simple transformation?

!!! question "Variation"

    Could Euler-Lagrange be used to find the minimising $\alpha$ or minimised $T$?
